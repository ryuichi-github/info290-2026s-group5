% INFO 290 — Group 5 Final Report (DRAFT v4)
% Two-column format. Upload to Overleaf directly.
% Search \TODO and \VERIFY for all pending team action items.
\documentclass[10pt,twocolumn]{article}

\usepackage[margin=0.75in]{geometry}
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{hyperref}
\usepackage{enumitem}
\usepackage{xcolor}
\usepackage{amsmath}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{natbib}
\usepackage{titlesec}
\usepackage{float}
\usepackage{tabularx}
\usepackage{multirow}
\usepackage{array}

% Spacing for 5-page limit
\setlength{\parskip}{3pt}
\setlength{\parindent}{1em}
\setlength{\columnsep}{0.25in}
\titlespacing*{\section}{0pt}{10pt}{4pt}
\titlespacing*{\subsection}{0pt}{7pt}{2pt}
\titlespacing*{\subsubsection}{0pt}{5pt}{2pt}

% Draft markers
\newcommand{\TODO}[1]{\textcolor{red}{\textbf{[TODO: #1]}}}
\newcommand{\VERIFY}[1]{\textcolor{red}{\textbf{[VERIFY: #1]}}}

\title{\Large Utilization of RAG and Agentic AI\\for Marketing Asset Generation}
\author{
  Ryuichi Ikeda, Priya Charagondla, Shivani Parikh, Nseke Ngilbus, Lena Ehrenreich \\
  University of California, Berkeley \\
  Berkeley, CA 94720, USA \\
  \texttt{\{i.ryuichi,priya.charagondla,neomishivani,nngilbus,????\}@berkeley.edu}
}
\date{}

\begin{document}
\maketitle
% ══════════════════════════════════════════════════════════════
% NOTE FOR TEAM: Color coding in this draft
% ══════════════════════════════════════════════════════════════
\begin{center}
\fbox{\begin{minipage}{0.9\columnwidth}
\small
\textbf{Draft color coding — remove before final submission.}\\[4pt]
Black = established facts, system descriptions, methodology (fixed).\\
\textcolor{blue}{Blue = findings supported by current data. Direction is solid, but wording may be refined. Convert to black in final version.}\\
\textcolor{red}{Red = claims that depend on pending experiments (rubric tightening, re-evaluation, statistical tests). Must be updated or confirmed before submission.}\\[4pt]
\end{minipage}}
\end{center}

% ══════════════════════════════════════════════════════════════
\begin{abstract}
We present a system that generates professional marketing assets for films—taglines, promotional overviews, and poster art direction—by retrieving thematically similar films from a corpus of 4,312 TMDB movies and injecting their metadata and poster visual descriptions into the generation prompt.
We compare three system variants: a zero-shot baseline~(V1), retrieval-augmented generation with visual grounding via pre-computed poster captions~(V2), and V2 extended with an iterative refinement loop using a separate LLM-as-Judge~(V3).
Outputs are evaluated on five dimensions by GPT-4o, using a rubric designed around failure modes observed during development.

Our investigation yields two central findings.
\textcolor{blue}{First, a cross-generator comparison shows that the RAG benefit is model-dependent: a larger model benefits from retrieved context while a 7B-parameter model does not,}
\textcolor{red}{and on a held-out test set ($n{=}36$), V2 outperforms V1, with the clearest gain in visual specificity.}
% ^^^ RED: test set numbers may change after prompt/rubric improvements.

\textcolor{blue}{Second, automated evaluation of creative outputs—where no ground truth exists—poses unique challenges:}
\textcolor{red}{we document ceiling effects in three of five rubric dimensions that limit discriminative power between variants.}
% ^^^ RED: ceiling effect severity may change if rubric is tightened.
\end{abstract}

% ══════════════════════════════════════════════════════════════
\section{Introduction}

Film marketing assets—taglines, promotional synopses, and poster art direction—are produced through iterative collaboration between creative teams, market analysts, and studio executives. This process is slow, expensive, and relies on tacit knowledge of genre conventions that is difficult to codify. While large language models (LLMs) can generate creative text, their zero-shot outputs typically lack two properties that professional marketing demands: \textit{strategic grounding} in proven industry patterns, and \textit{genre-specific visual vocabulary} for art direction.

We address this gap by building a system that takes a \textit{structured movie concept} (premise, thematic core, and constraints) and uses \textit{retrieval-augmented generation with visual grounding} to produce \textit{marketing assets}: a tagline, a promotional overview, and poster art direction. The film industry serves as a concrete evaluation domain, but the architecture generalizes to any domain where creative assets must balance originality with adherence to genre convention.

Our approach is guided by two hypotheses. First, grounding generation in historical patterns from thematically similar films—including visual descriptions of their poster art—will improve output quality beyond what zero-shot generation achieves. Second, iterative refinement through an LLM-as-Judge feedback loop can further improve quality by identifying and correcting specific weaknesses. Figure~\ref{fig:journey} illustrates the end-to-end user journey.

% --- FIGURE 1 PLACEHOLDER ---
\begin{figure}[t]
\centering
\fbox{\begin{minipage}{0.9\columnwidth}
\centering
\vspace{3cm}
\textcolor{gray}{\textbf{End-to-End User Journey}\\[4pt]
\small Insert \texttt{figure1\_user\_journey.svg}\\
User $\to$ Input $\to$ [Retrieval $\to$] Generation [$\to$ Refinement] $\to$ Output.}
\vspace{3cm}
\end{minipage}}
\caption{End-to-end user journey. A creative professional provides a structured movie concept. The system generates marketing assets, optionally using retrieval (V2/V3) and judge feedback (V3).}
\label{fig:journey}
\end{figure}

Section~2 reviews related work. Section~3 describes data sources. Section~4 presents the system architecture and three variants. Section~5 details experiments and evaluation. Section~6 discusses findings, and Section~7 concludes.

% ══════════════════════════════════════════════════════════════
\section{Related Work}

\citet{sun2025ai} compare LLM-generated and human-written marketing slogans, finding comparable consumer engagement but noting that LLM outputs tend toward structural homogeneity and lack market-specific nuance. This identifies a core limitation that our work addresses: without external grounding, LLMs produce generic creative text that lacks the genre-specific conventions professionals rely on. Our RAG pipeline provides this grounding by retrieving stylistic patterns from historically successful films, and our structured input format with negative constraints gives the generator explicit boundaries to combat homogeneity.

Self-Refine \citep{madaan2023selfrefine} demonstrates that LLMs can iteratively improve their own outputs through self-generated feedback, with diminishing returns beyond 2--3 iterations. Our V3 variant adopts this judge-then-refine architecture, but with a key modification: we separate the judge model (GPT-4o) from the generator (Mistral-7B). Same-model evaluation has been shown to inflate scores \citep{zheng2023judging}; we believe this risk is amplified in creative tasks, where quality is subjective and self-evaluation may confirm the generator's own stylistic tendencies.

% ══════════════════════════════════════════════════════════════
\section{Data Sources and Inputs}

\subsection{TMDB Movie Corpus}
We collected metadata for 4,816 movies from the TMDB (The Movie Database) API, including title, tagline, overview, genre IDs, and audience metrics. After removing 504 records (10.5\%) with missing taglines, the working corpus contains \textbf{4,312 movies}. The genre distribution is broad but skewed toward Drama~(1,845) and Action~(1,414), followed by Thriller~(1,366), Comedy~(1,221), and Horror~(709). Our experimental case studies were selected from the most represented genres to ensure adequate retrieval coverage.

Figure~\ref{fig:tmdb_sample} shows a representative record from the corpus.

\begin{figure}[h]
\centering
\fbox{\begin{minipage}{0.95\columnwidth}
\small
\textbf{Title:} Take Shelter (2011) \\
\textbf{Genre:} Drama, Thriller \\
\textbf{Tagline:} ``Is it real or just a dream?'' \\
\textbf{Overview:} ``A working-class husband and father questions whether his terrifying dreams of an apocalyptic storm signal something real, or the onset of mental illness.'' \\
\textbf{Popularity:} 18.2 \quad \textbf{Vote avg:} 7.1 \\
\textbf{Poster:} \textit{(see Figure~\ref{fig:vlm})}
\end{minipage}}
\caption{Sample TMDB record. Each movie includes textual metadata (used for retrieval embedding) and a poster image (used for visual caption generation).}
\label{fig:tmdb_sample}
\end{figure}

\subsection{Visual Grounding via Poster Captions}
Early experiments revealed that without visual references, generated poster art direction was generic and lacked compositional detail—e.g., outputs like ``dark and moody poster'' that no designer could act on. To address this, we pre-computed structured visual descriptions for all 4,312 movie posters.

We evaluated three vision-language models across four configurations (Figure~\ref{fig:vlm}) before selecting Gemini~2.5~Flash, which produces structured prose across six visual axes: composition, lighting, color palette, mood, typography, and symbolic elements. Captions were pre-computed for all posters via the Gemini API prior to any experiments and stored as an additional column in the retrieval corpus, requiring no API calls at inference time.
\begin{figure}[t]
\centering
\includegraphics[width=0.35\columnwidth]{figures/shelter.png}

\vspace{0.5em}
\small
\begin{tabular}{p{1.7cm}p{1.1cm}p{3.6cm}}
\toprule
\textbf{Model} & \textbf{Verdict} & \textbf{Example Output} \\
\midrule
vit-gpt2 & Rejected & ``man standing in front of large group of birds'' \\
BLIP-2 (zero-shot) & Rejected & ``the poster is dark and moody'' \\
BLIP-2 (Q\&A) & Marginal & ``a man and woman in front of a stormy sky'' \\
\textbf{Gemini 2.5 Flash} & \textbf{Adopted} & ``A man with a concerned upward gaze\ldots two funnel clouds on the horizon\ldots'' \\
\bottomrule
\end{tabular}
\caption{VLM captioning comparison on the \textit{Take Shelter} (2011) poster (above). Gemini~2.5~Flash was selected for descriptive richness and accuracy.}
\label{fig:vlm}
\end{figure}

% ══════════════════════════════════════════════════════════════
\section{System Architecture and Variants}

% --- FIGURE 2 PLACEHOLDER ---
\begin{figure}[t]
\centering
\fbox{\begin{minipage}{0.9\columnwidth}
\centering
\vspace{3cm}
\textcolor{gray}{\textbf{System Architecture}\\[4pt]
\small Insert \texttt{figure2\_system\_architecture.svg}\\
V1 / V2 / V3 comparison.}
\vspace{3cm}
\end{minipage}}
\caption{System architecture. V1 generates from input alone. V2 adds retrieved references and poster captions. V3 extends V2 with an iterative judge-feedback loop.}
\label{fig:architecture}
\end{figure}

We implemented three system variants (Figure~\ref{fig:architecture}) that share a common output format—a JSON object with tagline, overview, and poster art direction—and differ in how the generation prompt is constructed. Table~\ref{tab:variants} summarizes their components.

\begin{table}[h]
\centering
\small
\caption{System variant components.}
\label{tab:variants}
\begin{tabular}{lcccc}
\toprule
\textbf{Variant} & \textbf{Input} & \textbf{Retrieval} & \textbf{Gen.} & \textbf{Refine} \\
\midrule
V1 (Zero-Shot) & \checkmark & — & \checkmark & — \\
V2 (RAG+Visual) & \checkmark & \checkmark & \checkmark & — \\
V3 (Agentic) & \checkmark & \checkmark & \checkmark & \checkmark \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Input Design}
Early experiments with free-text prompts produced \textit{narrative contamination}: the generator invented characters, villains, and plot events not specified by the user (e.g., fabricating a disappearance subplot and a villain for a premise about a flooded clinic). We replaced free-text input with a three-field structured format:

\begin{itemize}[nosep,leftmargin=*]
\item \texttt{core\_premise}: what happens in the film.
\item \texttt{thematic\_core}: what the film means—its central tension.
\item \texttt{negative\_constraints}: what the generator must \textit{not} do.
\end{itemize}

With the structured format, the generator no longer invents plot elements excluded by the user's constraints. This design is shared across all three variants.

\subsection{Retrieval (V2, V3)}
Each movie in the corpus is represented by concatenating its title, tagline, and overview, then embedding with Sentence-BERT (all-MiniLM-L6-v2, 384~dim) and indexing in FAISS. At query time, the user's premise is embedded and matched against this index, returning the top-$k$ most similar films (where $k$ is the number of retrieved references, determined by hyperparameter tuning in Section~5.4). For each retrieved film, a reference block is assembled containing its tagline, overview, and pre-computed poster caption.

Early experiments showed that retrieval significantly improved stylistic diversity compared to zero-shot generation, but also introduced risks of \textit{narrative contamination from retrieved plots}—the generator would borrow character names or story events from retrieved references. To prevent this, the prompt explicitly instructs the generator to use references for \textit{stylistic and tonal guidance only}, not narrative content.

\subsection{Generation (All Variants)}
The generator receives the structured input and (for V2/V3) retrieved reference blocks, and produces a JSON object containing three marketing assets: a tagline, a promotional overview, and poster art direction. The prompt instructs the generator to write the overview as compelling promotional copy rather than a plot summary, and to specify concrete visual elements (subject position, lighting, palette, symbolic objects) in the art direction. We use Mistral-7B-Instruct-v0.2 with 4-bit quantization as the base generator. A post-processing step parses the raw output to extract and validate the JSON structure. Implementation details are provided in Appendix~B.

\subsection{Agentic Refinement (V3)}
V3 adds an iterative refinement loop after generation. The output is scored by GPT-4o on five dimensions (Section~5.2). If any dimension scores below~3, the judge identifies the weakest dimension and provides a reason; this feedback is appended to the prompt, and the generator produces a new output. The loop runs for at most 3~iterations, with early stopping when all dimensions pass. The judge model (GPT-4o) is separate from the generator (Mistral-7B)—the rationale for this separation is discussed in Section~2.

% ══════════════════════════════════════════════════════════════
\section{Experiments and Evaluation}

\subsection{Evaluation Overview}

% ── FOR TEAM: This subsection explains what we did, in what order, and why.
% ── The sequence follows our actual research process, not a textbook layout.

Marketing assets are creative artifacts with no single correct answer. Our evaluation must assess whether outputs are \textit{well-crafted}---anchored to the premise, tonally appropriate, visually specific, and creatively distinctive---without comparing to a gold standard.

Our evaluation developed iteratively through the following steps:

\begin{enumerate}[nosep,leftmargin=*]
\item \textbf{Rubric design} (Section~5.2): We defined a 5-dimension scoring rubric based on failure modes observed during early development.
\item \textbf{Case study} (Section~5.3): We tested V1/V2/V3 on three hand-crafted movie ideas ($n{=}3$). The variants showed little difference in scores. This raised the question: \textit{why doesn't RAG help?}
\item \textbf{Generator comparison} (Section~5.4): To determine whether the problem was the RAG approach or the base model, we ran the same three ideas through two additional generators. This suggested that Mistral-7B may lack the capacity to leverage retrieved context.
\item \textbf{Scaled evaluation data} (Section~5.5): Since $n{=}3$ cannot support statistical claims, we generated 200 synthetic evaluation inputs from TMDB metadata, filtered to 181, and split into validation ($n{=}145$) and test ($n{=}36$) sets.
\item \textbf{Hyperparameter tuning} (Section~5.6): Grid search on the validation set identified the best retrieval and generation configuration.
\item \textbf{Test set evaluation} (Section~5.7): V1/V2/V3 were evaluated on the held-out test set with the best configuration. V2 showed a small overall advantage, with the clearest gain in visual specificity.
\item \textbf{Ceiling effect analysis} (Section~5.8): Examining the test set results revealed that 3 of 5 rubric dimensions score near ceiling for all variants, limiting discriminative power.
\end{enumerate}

\textcolor{red}{Four additional analyses are planned:
\begin{itemize}[nosep,leftmargin=*]
\item[\textbf{A.}] Statistical significance test (Wilcoxon signed-rank) on V1 vs V2 per-record scores.
\item[\textbf{B.}] Rubric tightening: re-judge existing outputs with stricter anchors to test the ceiling effect.
\item[\textbf{C.}] Generator comparison on a larger sample ($n{=}20$) to strengthen the model-capacity finding.
\item[\textbf{D.}] Prompt: add genre-aware craft guidance.
\end{itemize}}

\subsection{Evaluation Rubric}
We defined five evaluation dimensions, each motivated by a specific failure mode observed during development:

\begin{enumerate}[nosep,leftmargin=*]
\item \textbf{Narrative fidelity}: Does the output follow the premise without inventing plot elements excluded by constraints? \textit{(Motivated by narrative contamination observed with free-text inputs.)}
\item \textbf{Genre alignment}: Do vocabulary and emotional register match the target genre? \textit{(Motivated by tone drift in early experiments.)}
\item \textbf{Visual specificity}: Does the poster direction include concrete, actionable visual elements---subject position, lighting, palette, symbolic objects? \textit{(Motivated by generic outputs lacking compositional detail.)}
\item \textbf{Creative specificity}: Does the tagline contain distinctive, non-interchangeable phrasing? \textit{(Motivated by the homogeneity problem identified by \citet{sun2025ai}.)}
\item \textbf{Output format validity}: Are all JSON fields present and within specified word limits?
\end{enumerate}

Each dimension is scored 1--5 by GPT-4o, with rubric anchors at scores~1, 3, and~5 (full rubric in Appendix~A). Scores~2 and~4 are interpolated. The judge model is deliberately different from the generator to prevent self-evaluation bias \citep{zheng2023judging}.


\subsection{Case Study ($n{=}3$)}

We began evaluation with three hand-crafted movie concepts: a Psychological Thriller, a Drama about family and loss, and an Action Thriller. Each concept was defined with the structured input format (premise, theme, constraints) and run through all three pipeline variants (V1/V2/V3) once each. These case studies served two purposes: identifying failure modes that informed the rubric design, and providing qualitative illustrations of inter-variant differences.

\begin{table}[h]
\centering
\small
\caption{Case study results ($n{=}3$, Mistral-7B, single run each). LLM-as-Judge average scores.}
\label{tab:casestudy}
\begin{tabular}{lccc}
\toprule
\textbf{Idea} & \textbf{V1} & \textbf{V2} & \textbf{V3} \\
\midrule
Idea 1 --- Psych.\ Thriller & 4.20 & 4.20 & 4.20 \\
Idea 2 --- Drama & 4.60 & 4.20 & 4.20 \\
Idea 3 --- Action Thriller & 4.60 & 4.20 & 3.80 \\
\bottomrule
\end{tabular}
\end{table}

At face value, V1 (zero-shot) performed equal to or better than V2 and V3 in all three cases. This was unexpected: if RAG provides useful grounding, V2 should outperform V1. This observation raised two hypotheses: (a)~the base model (Mistral-7B) may lack the capacity to leverage retrieved context, or (b)~the evaluation rubric may not be sensitive enough to detect real quality differences. Sections~5.4 and~5.8 investigate these hypotheses respectively.

\begin{table*}[t!]
\centering
\small
\caption{Input concept, generated outputs, and poster images for Idea~1 (Psychological Thriller). All variants scored 4.20/5.00 overall by LLM-as-Judge, despite visible qualitative differences in tagline specificity, poster art direction detail, and resulting image composition.}
\label{tab:idea1_outputs}
\begin{tabularx}{\textwidth}{l>{\raggedright\arraybackslash}X}
\toprule
\textbf{Input} & \textbf{Premise:} A man wakes up in a hospital with no memory of his past. People claiming to be his family and friends appear one by one---each with a different account of who he was. As fragments of memory return, he suspects someone is controlling what he remembers. \newline \textbf{Theme:} Identity is not what you remember---it is what others need you to believe. The horror of discovering that your own past makes you the monster. \newline \textbf{Constraints:} Do not reveal whether visitors are real or fabricated. No action sequences or physical violence. Memory recovery should feel terrifying, not heroic. \\
\bottomrule
\end{tabularx}

\vspace{-0.5em}

\begin{tabularx}{\textwidth}{l>{\raggedright\arraybackslash}X>{\raggedright\arraybackslash}X>{\raggedright\arraybackslash}X}
\toprule
 & \textbf{V1 (Zero-Shot)} & \textbf{V2 (RAG+Visual)} & \textbf{V3 (Agentic)} \\
\midrule
\textbf{Poster Image} &
\includegraphics[width=\linewidth]{figures/idea1_v1.png} &
\includegraphics[width=\linewidth]{figures/idea1_v2.png} &
\includegraphics[width=\linewidth]{figures/idea1_v3.png} \\
\midrule
\textbf{Tagline} & ``Unraveling the Threads of Identity'' & ``Unraveling Memories'' & ``Who controls your past?'' \\
\midrule
\textbf{Overview} & A man's shattered past resurfaces in a labyrinthine maze of memories, each more deceptive than the last. Reality unravels as he grapples with the chilling truth behind his identity. What if your past was a prison? & Lost in a labyrinth of memories, a man grapples with a shocking revelation: his past may be a twisted tapestry of lies. Haunted by visions and visited by enigmatic figures, he embarks on a harrowing quest for the truth. & A man regains consciousness in a hospital, unsure of his past. Friends and family visit, but their stories contradict one another. As his memories resurface, he uncovers a sinister force manipulating his recollections. \\
\midrule
\textbf{Poster Art Direction} & An enigmatic figure stands at the edge of a dimly lit room, a shattered mirror reflecting distorted memories behind him. His reflection reveals only an endless maze of mirrors. & A haunting portrait of a man's fractured psyche, featuring an expressionless figure surrounded by shattered mirrors reflecting distorted faces. & A blurred image of a man in a hospital bed, as figures from his past crowd around him. Their faces are obscured by shadows. \\
\midrule
\textbf{LLM-as-Judge} & 4.20 & 4.20 & 4.20 \\
\bottomrule
\end{tabularx}
\end{table*}

\subsection{Generator Comparison: Is RAG or the Model the Bottleneck?}

The case study results raised hypothesis~(a): Mistral-7B may be too small to leverage retrieved context. To test this, we ran the same three case study ideas through two additional generators (Gemini~2.5~Flash and GPT-4o-mini) and compared the RAG delta (V2 score minus V1 score) across all three models (Table~\ref{tab:generator}).

\begin{table}[h]
\centering
\small
\caption{RAG effect by generator. avg\_delta = mean(V2$-$V1) across 3 case study ideas. Evaluator: GPT-4o (fixed). $n{=}3$; interpret as directional evidence.}
\label{tab:generator}
\begin{tabular}{lccr}
\toprule
\textbf{Generator} & \textbf{V1 avg} & \textbf{V2 avg} & \textbf{avg\_$\delta$} \\
\midrule
Mistral-7B & 3.80 & 3.47 & $-$0.33 \\
Gemini 2.5 Flash & 4.73 & 4.93 & +0.20 \\
GPT-4o-mini & 4.20 & 4.20 & 0.00 \\
\bottomrule
\end{tabular}
\end{table}

\textcolor{blue}{Gemini~2.5~Flash, a substantially larger model, consistently benefits from RAG, while Mistral-7B does not. This suggests that RAG effectiveness depends on the generator's capacity to utilize retrieved context---a negative result with a small model should not be interpreted as evidence against the RAG approach itself, but as evidence of a model-capacity threshold.}

However, this comparison was conducted on only $n{=}3$ ideas, limiting statistical power. We treat this as directional evidence rather than a definitive finding.

\textcolor{red}{Planned: re-run the generator comparison (Gemini 2.5 Flash and GPT-4o-mini) on a larger sample from the validation set ($n{=}20$) to test whether the model-capacity finding holds beyond the 3 case study ideas (see planned analysis C).}

\subsection{Evaluation Data: Scaled QA Pairs}

The case study ($n{=}3$) and generator comparison provided directional insight, but cannot support statistical claims. To enable quantitative evaluation, we used GPT-4o to \textit{reverse-generate} structured movie inputs from TMDB metadata. The task: given a real film's title, genre, and overview, generate the structured input (\texttt{core\_premise}, \texttt{thematic\_core}, \texttt{negative\_constraints}) that would produce it. This yielded 200 synthetic inputs.

A Mistral-7B quality critic then reviewed each input for internal coherence, rejecting cases where the generated premise was self-contradictory or too vague to produce meaningful marketing assets. This retained 181 validated pairs (19 rejected). These were split by stratified sampling on primary genre into a validation set ($n{=}145$) and a held-out test set ($n{=}36$).

All subsequent tuning and iteration was conducted exclusively on the validation set. The test set was evaluated once with the final configuration.

\subsection{Hyperparameter Tuning}

We performed a grid search on the validation set over three parameters: retrieval count $k \in \{3,5,7\}$, genre filtering $\in \{\text{True, False}\}$, and generation temperature $\in \{0.4, 0.6, 0.8\}$, yielding 18~configurations. Due to generation cost (each configuration requires running the full pipeline per record), each was evaluated on a random sample of 20~validation records. The best configuration was $k{=}3$, genre\_filter=False, temperature=0.6 (avg LLM-as-Judge score = 4.50). Notably, genre filtering \textit{reduced} performance, likely because restricting retrieval to same-genre films narrows stylistic diversity.

\subsection{Test Set Evaluation ($n{=}36$)}

Table~\ref{tab:testset} reports results on the held-out test set using the best hyperparameter configuration. All three variants use Mistral-7B as generator.

\begin{table}[h]
\centering
\small
\caption{Test set evaluation ($n{=}36$, $k{=}3$, genre\_filter=False, temp=0.6). All five dimensions shown.}
\label{tab:testset}
\begin{tabular}{lcccccc}
\toprule
 & \textbf{Avg} & \textbf{Narr.} & \textbf{Genre} & \textbf{Vis.} & \textbf{Cre.} & \textbf{Fmt.} \\
\midrule
V1 & 4.42 & 5.00 & 4.86 & 3.81 & 3.42 & 5.00 \\
V2 & \textbf{4.46} & 4.97 & \textbf{4.97} & \textbf{4.08} & 3.39 & 4.86 \\
V3 & 4.42 & 5.00 & 4.94 & 4.00 & 3.22 & 4.94 \\
\bottomrule
\end{tabular}
\end{table}


\textcolor{blue}{V2 achieves the highest overall score (4.46). The clearest improvement is in visual specificity: V2 scores 4.08 versus V1's 3.81, a +0.27 gain. This suggests that poster captions provide concrete visual vocabulary---compositional elements, lighting descriptions, color palettes---that the generator incorporates into its art direction output.}

However, the overall gap is small (0.04), and must be interpreted in light of two factors. First, the generator comparison (Section~5.4) suggested that Mistral-7B is a weak RAG consumer; the positive delta observed with Gemini~2.5~Flash suggests that a stronger generator would show a larger V2 advantage. Second, the rubric exhibits a ceiling effect (Section~5.8) that compresses score distributions.

\textcolor{blue}{V3 does not outperform V2---consistent with the generator comparison: if Mistral-7B cannot effectively leverage retrieved context in the prompt, it is unlikely to effectively leverage natural-language feedback from the judge, which places even greater demands on instruction-following ability.}

\textcolor{red}{Planned: run Wilcoxon signed-rank test on V1 vs V2 per-record scores and report p-value (see planned analysis A).}

\VERIFY{If prompt/rubric improvements lead to a re-evaluation, update this table.}

\subsection{Ceiling Effect Analysis}

Three of five rubric dimensions score near ceiling across all variants: narrative fidelity (V1:~5.00, V2:~4.97, V3:~5.00), genre alignment (4.86--4.97), and format validity (4.86--5.00).

This is both a positive and a negative observation. On one hand, it confirms that all variants produce outputs faithful to the premise, tonally appropriate, and correctly formatted---the structured input design (Section~4.1) effectively prevents narrative contamination. On the other hand, near-ceiling scores provide no discriminative power between variants. The overall score differences are driven entirely by visual specificity and creative specificity, giving these two dimensions outsized influence on the aggregate.

The tagline example from Section~5.3, where three qualitatively different outputs received identical scores (4.20), provides concrete evidence that the current rubric under-discriminates. This is hypothesis~(b) from Section~5.3: we have identified the phenomenon and a likely cause (rubric sensitivity), but have not yet tested whether tightening the rubric resolves it. Further interpretation is provided in Section~6.

\textcolor{red}{Planned: re-judge 20 val records with tightened rubric anchors and compare score distributions to current results (see planned analysis B).}


% ══════════════════════════════════════════════════════════════
\section{Discussion}

Our experiments produced a clear pattern: V2 (RAG + Visual Grounding) outperforms V1 (Zero-Shot) on the dimension most dependent on external knowledge (visual specificity, +0.27), while V3 (Agentic Refinement) fails to improve over V2. We discuss three interconnected factors that explain this pattern.

\textbf{Model capacity as the binding constraint.}
\textcolor{blue}{The generator comparison (Section~5.4) and test set results (Section~5.7) point to a consistent explanation: Mistral-7B at 7B parameters lacks the capacity to fully exploit either retrieved context (V2) or natural-language feedback (V3).} RAG requires the model to read reference material, extract stylistic patterns, and apply them without copying narrative content. Agentic refinement demands even more: parsing meta-level feedback about its own output and selectively revising specific dimensions. Both tasks require strong instruction-following ability that scales with model size. \textcolor{blue}{The positive RAG delta observed with Gemini~2.5~Flash (+0.20, Section~5.4) supports this interpretation}---though the comparison is based on $n{=}3$ ideas and should be treated as directional evidence. \textcolor{red}{Testing with a stronger local generator (see planned analysis C, D) would directly confirm or refute this hypothesis.}

\textbf{Evaluation sensitivity and the ceiling effect.}
\textcolor{blue}{Three of five rubric dimensions score near ceiling ($\geq$4.86) for all variants, eliminating their discriminative value (Section~5.8).} This means the overall V2 advantage (4.46 vs.\ 4.42) rests entirely on visual specificity and creative specificity---two dimensions out of five. The root cause is rubric calibration: anchors like ``vocabulary fully matches the target genre'' (genre alignment, score~5) are achievable by any competent LLM. More demanding functional anchors---e.g., ``this poster direction contains enough detail that a designer could begin sketching immediately''---would push scores downward and widen inter-variant variance. The tagline example from Section~5.3, where three qualitatively different outputs received identical scores, illustrates this concretely. \textcolor{red}{Re-judging with tightened rubric anchors (planned analysis B) would test whether the small V2 advantage is an artifact of rubric insensitivity or a genuine ceiling on what RAG adds.}

\textbf{Evaluation methodology: trade-offs and biases.}
GPT-4o serves as both the QA input generator (Section~5.5) and the judge (Section~5.2), creating a potential circularity: the judge may favor input phrasings consistent with its own generation tendencies. Additionally, the test set is small ($n{=}36$), a consequence of the synthesis-filtering-splitting pipeline (200~$\to$~181~$\to$~36). This limits the statistical power of inter-variant comparisons. \textcolor{red}{A paired statistical test (planned analysis A) will quantify whether the V2 advantage is significant at this sample size.}

\textbf{Practical trade-offs.}
V1 is simplest (\textasciitilde5s, no API cost). V2 adds \textasciitilde2s of local retrieval with no runtime API cost (FAISS search over pre-embedded corpus, pre-computed captions). V3 adds 20--90s of latency and \textasciitilde\$0.02/run for GPT-4o judge calls, with no measurable quality gain in the current configuration. With Mistral-7B as generator, V3's cost-benefit ratio is negative.
% ══════════════════════════════════════════════════════════════
\section{Conclusions and Future Work}

We presented a RAG-augmented system for generating film marketing assets, comparing zero-shot (V1), retrieval-augmented with visual grounding (V2), and agentic refinement (V3) variants. Our findings center on two themes:

\textcolor{blue}{\textbf{RAG works, but model capacity is the binding constraint.} V2 improves output quality over V1, most clearly in visual specificity (+0.27), suggesting that retrieved poster captions provide actionable grounding for art direction. However, this benefit is model-dependent: Mistral-7B (7B parameters) shows a negative RAG delta while Gemini~2.5~Flash shows a positive one, pointing to a capacity threshold for effective context utilization. V3's agentic refinement loop, which demands even stronger instruction-following, does not improve over V2 with Mistral-7B.}

\textbf{Evaluating creative outputs without ground truth requires careful methodology.} Our rubric exhibits ceiling effects in three of five dimensions, limiting discriminative power. The overall V2 advantage (4.46 vs.\ 4.42) is small and rests on only two dimensions. Rubric design---not just system design---determines what quality differences can be detected.

\textbf{Limitations.} The test set is small ($n{=}36$), a consequence of the synthesis-filtering-splitting pipeline (200~$\to$~181~$\to$~36). The generator comparison is based on $n{=}3$ ideas. The rubric has ceiling effects in three of five dimensions. GPT-4o's dual role as input generator and judge introduces potential distributional bias.

\textbf{Future work}, ordered by expected impact:
\begin{enumerate}[nosep,leftmargin=*]
\item Test with a stronger local generator to confirm the model-capacity hypothesis---this is the most impactful open question.
\item Tighten rubric anchors to increase discriminative power---the most immediately executable improvement.
\item Conduct human evaluation (blind A/B/C) to validate LLM-as-Judge reliability.
\item Add prompt-level improvements (genre-aware craft guidance, pattern abstraction from retrieved references) to better exploit retrieved context.
\end{enumerate}

% ══════════════════════════════════════════════════════════════
\section*{Contributions}

\begin{table}[h]
\centering
\small
\caption*{Contribution breakdown. Per course requirement, each member contributes to both the System Architecture (Sec.~4) and Experiments (Sec.~5) sections.}
\begin{tabularx}{\columnwidth}{l>{\raggedright\arraybackslash}X>{\raggedright\arraybackslash}X}
\toprule
 & \textbf{Sections Written} & \textbf{Code / Notebook} \\
\midrule
Ryuichi & \textcolor{red}{TBU} & \textcolor{red}{TBU} \\
\midrule
Priya & \textcolor{red}{TBU} & \textcolor{red}{TBU} \\
\midrule
Shivani & \textcolor{red}{TBU} & \textcolor{red}{TBU} \\
\midrule
Nseke & \textcolor{red}{TBU} & \textcolor{red}{TBU} \\
\midrule
Lena (Ehrenreich) & \textcolor{red}{TBU} & \textcolor{red}{TBU} \\
\bottomrule
\end{tabularx}
\end{table}

\TODO{Finalize writing assignments. Ensure each member has at least one notebook where they are the primary contributor.}

% ══════════════════════════════════════════════════════════════
\bibliographystyle{plainnat}
\begin{thebibliography}{9}

\bibitem[Sun et~al.(2025)]{sun2025ai}
Sun, Y., et~al. (2025).
AI-Driven Copywriting: Comparing Consumer Engagement of Human vs.\ Machine-Generated Slogans.
\textcolor{red}{\textbf{[VERIFY: Confirm exact venue/journal and page numbers.]}}

\bibitem[Madaan et~al.(2023)]{madaan2023selfrefine}
Madaan, A., et~al. (2023).
Self-Refine: Iterative Refinement with Self-Feedback.
\textit{NeurIPS 2023}.

\bibitem[Zheng et~al.(2023)]{zheng2023judging}
Zheng, L., et~al. (2023).
Judging LLM-as-a-Judge with MT-Bench and Chatbot Arena.
\textit{NeurIPS 2023}.

\end{thebibliography}

% ══════════════════════════════════════════════════════════════
\clearpage
\onecolumn
\appendix
\section*{Appendix}

\subsection*{A. Full Evaluation Rubric}

\begin{table}[h]
\centering
\small
\caption*{LLM-as-Judge rubric. Scores 2 and 4 are interpolated between anchors. Judge model: GPT-4o (separate from generator).}
\begin{tabularx}{\textwidth}{p{2.3cm}XXX}
\toprule
\textbf{Dimension} & \textbf{Score 5} & \textbf{Score 3} & \textbf{Score 1} \\
\midrule
Narrative Fidelity & No constraints violated. All content strictly anchored to premise and theme. & Minor trope drift; no invented characters or major additions. & Invented characters or plot elements excluded by constraints. \\
\midrule
Genre Alignment & Vocabulary, rhythm, and register fully match genre. & Mostly aligned; one off-genre phrase or image. & Clearly wrong tone. \\
\midrule
Visual Specificity & $\geq$3 of: subject position, lighting, palette, shadow/silhouette, symbolic object. A designer could start sketching. & Atmosphere mentioned but $\geq$2 specifics missing. & Only generic descriptors (``dark and moody''). \\
\midrule
Creative Specificity & Unexpected word combination; memorable on first read. Not interchangeable with other films in this genre. & Competent but interchangeable with many films. & Clich\'{e} phrase from common marketing templates. \\
\midrule
Format Validity & All fields present, within word limits. & One field $\leq$20\% over limit. & Field missing or $>$20\% over. \\
\bottomrule
\end{tabularx}
\end{table}


\subsection*{B. Implementation Details}

\textbf{Generator:} Mistral-7B-Instruct-v0.2, 4-bit NF4 quantization via bitsandbytes, run on Google Colab T4 GPU (16GB VRAM). Sampling parameters: temperature=0.6, top-$p$=0.9, repetition penalty=1.10, max new tokens=320.

\textbf{Retrieval:} FAISS IndexFlatIP (cosine similarity on L2-normalized embeddings). Embedding model: Sentence-BERT all-MiniLM-L6-v2 (384 dimensions).

\textbf{Judge:} GPT-4o via OpenAI API, \texttt{response\_format=json\_object}, max\_tokens=1000.

\textbf{Poster captioning:} Gemini 2.5 Flash via Google AI API. 6-axis prose prompt covering composition, lighting, color palette, mood, typography, and symbolic elements.

\textbf{Prompt template:}

\TODO{Insert final \texttt{build\_prompt()} template used in the test set evaluation.}



\end{document}